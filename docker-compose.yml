services:
  cryptoscreener:
    build: .
    ports:
      - "9090:9090"
    command: ["--top", "5", "--metrics-port", "9090"]
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9091:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./monitoring/alert_rules.yml:/etc/prometheus/alert_rules.yml:ro
