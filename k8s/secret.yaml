# TEMPLATE ONLY — do NOT commit real secrets.
#
# Preferred: create the actual secret via kubectl:
#   kubectl create secret generic cryptoscreener-secrets \
#     --from-literal=BINANCE_API_KEY=<key> \
#     --from-literal=BINANCE_SECRET_KEY=<secret> \
#     --from-literal=ANTHROPIC_API_KEY=<key>
#
# Alternative: fill in stringData values below and apply:
#   kubectl apply -f k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: cryptoscreener-secrets
  labels:
    app: cryptoscreener
    app.kubernetes.io/name: cryptoscreener
    app.kubernetes.io/component: pipeline
type: Opaque
stringData:
  BINANCE_API_KEY: "REPLACE_ME"
  BINANCE_SECRET_KEY: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
